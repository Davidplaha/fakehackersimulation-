# Windows XP Simulator

A standalone browser-based Windows XP-style desktop simulator for prank/demo use.

Run locally:

```powershell
node server.js 8090
```

Then open:

```text
http://127.0.0.1:8090/
```

This is a visual simulator only. It does not access real operating system files or settings.
